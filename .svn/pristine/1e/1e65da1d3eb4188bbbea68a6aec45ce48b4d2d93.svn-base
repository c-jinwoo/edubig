
package com.sangs.util;

import javax.servlet.http.HttpSession;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import com.sangs.support.DataMap;
import com.sangs.support.SangsProperties;

import egovframework.com.cmm.EgovStringUtil;

/**
 * nxKey Util
 * << 개정이력(Modification Information) >>
 *
 *   수정일      	수정자           		수정내용
 *  -------    --------    		---------------------------
 *  2018.06.11	LSH				최초작성
 *
 * 
 *
 */
public class NxKeyUtil extends EgovStringUtil{


    private static Logger log = LoggerFactory.getLogger("com.sangs.util.SangsUtil");

    /**
     * nxKey 종단간암호화 - 복호화 실행
     * @param String hid_key_data 키 데이터
     * @param String targetVal 암호화 된 데이터
     * @param HttpSession session 세션
     * @return DataMap.errCd 에러코드 정상 - 0
     * @return DataMap.errMsg 에러메세지
     * @return DataMap.returnVal 복호화 데이터
     * @throws Exception
     */
    // TODO : nxKey 종단간암호화 - 복호화 실행
    public static DataMap getDecryptNxKey(String hid_key_data, String targetVal, HttpSession session) {
        DataMap nxMap = new DataMap();
    	return nxMap;
    }
}




